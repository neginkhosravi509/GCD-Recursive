GCD Recursive

A simple C# program to calculate the Greatest Common Divisor (GCD) of two numbers using a recursive method.

How it works

User inputs two numbers.

The program uses a recursive method to find their GCD.

The result is printed to the console.


Example

Input: 24, 36
Output: 12
